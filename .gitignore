# dependencies
/node_modules

# build output: dist/bot.js JE u repou (server ga samo pokrene, bez build koraka)

# secrets — NIKAD u commit (repoi su javni!)
.env
.env.*
!.env.example

# database / cache
*.db
*.db-wal
*.db-shm
/data

# runtime fajlovi (prave se na serveru, ne idu u git)
settings.json
predlozi.txt
prazne-pretrage.txt
watches.json
buyer-monitor.json

# klonirani referentni repoi (CRM + sajt) — nisu deo Fice
/_reference

# misc
.DS_Store
*.log
